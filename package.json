{
  "name": "saulo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "http-server . -p 4173 -c-1",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "validate": "npm run format:check && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1",
    "http-server": "^14.1.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.2",
    "prettier": "^3.6.2"
  },
  "lint-staged": {
    "*.{html,css,js,json,md,yml,yaml}": "prettier --write"
  }
}
